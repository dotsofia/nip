fn fib(n: number): number {
    if n == 0 || n == 1 {
        return n;
    }

    return fib(n - 2) + fib(n - 1);
}

fn main(): void {
    let i = 0;
    while i < 20 {
        display(fib(i));
        i = i + 1;
    }
}
